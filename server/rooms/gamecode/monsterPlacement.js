// Monster placement stub.
// Accepts a dungeon map string; returns a list of placed monsters (empty for now).

function placeMonsters(dungeonMap, options = {}) {
  // TODO: implement placement logic (rooms, corridors, difficulty scaling)
  return [];
}

module.exports = { placeMonsters };
